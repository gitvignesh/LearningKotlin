package com.karnamic.learningkotlin

object TwoPointers {

    /**
     * Determines if a given string is a palindrome.
     *
     * A phrase is considered a palindrome if, after converting all uppercase letters into lowercase letters
     * and removing all non-alphanumeric characters, it reads the same forward and backward.
     * Alphanumeric characters include letters and numbers.
     *
     * @param s The input string to check for palindrome.
     * @return `true` if the input string is a palindrome, `false` otherwise.
     */
    fun isPalindrome(s: String): Boolean {
        // Your implementation here
        TODO()
    }

}