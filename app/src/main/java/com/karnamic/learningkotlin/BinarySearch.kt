package com.karnamic.learningkotlin

import java.util.ArrayList

object BinarySearch {

    fun <E> ArrayList<E>.searchUsingBinarySearch(target: E): Int {

        return -1;
    }

    /**
     * Given a sorted rotated array nums of unique elements, returns the minimum element of this array.
     *
     * The array may be rotated between 1 and n times, where rotating an array [a[0], a[1], ..., a[n-1]]
     * 1 time results in the array [a[n-1], a[0], a[1], ..., a[n-2]].
     *
     * The algorithm must run in O(log n) time.
     *
     * @param nums The sorted rotated array of unique elements.
     * @return The minimum element in the array.
     */
    fun findMin(nums: IntArray): Int {
        // Your implementation here
        TODO()
    }


}